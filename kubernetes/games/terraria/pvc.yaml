apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: terraria-data
  namespace: terraria
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
